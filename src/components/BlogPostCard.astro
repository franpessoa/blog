---
const { title, mod, up, url, displayLang } = Astro.props;
---

<li>
    <a href={"/" + url}
        >{title} ({up}{mod ? " / mod " + mod : ""}) {
            displayLang ? <b>{"<" + displayLang + ">"}</b> : ""
        }</a
    >
</li>
